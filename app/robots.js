// SEO: robots.txt generado por Next. Permite todo y apunta al sitemap.
export default function robots() {
  return {
    rules: { userAgent: '*', allow: '/' },
    sitemap: 'https://velasdevas.cl/sitemap.xml',
  };
}
